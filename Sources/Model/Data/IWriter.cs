using Model.Games;
using Model.Players;

namespace Model.Data;

public partial interface IWriter : IDisposable
{
    // This interface defines write methods for Player, Game, Hand and Group.
    // Show the declaration of these methods in each partial class.
}